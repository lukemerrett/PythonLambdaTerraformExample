version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - sudo unzip build/terraform_1.0.9_linux_amd64.zip -d /bin
  build:
    commands:
      - terraform init
      - terraform apply
artifacts:
  files: 
    - package.zip